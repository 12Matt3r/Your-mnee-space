# Palette's Journal

This journal records critical UX and accessibility learnings.

## 2024-05-22 - Focus Visibility
**Learning:** `outline-none` on textareas removes accessibility for keyboard users unless replaced by `focus-visible` styles.
**Action:** Always pair `outline-none` with `focus-visible:ring` or similar indicators.
