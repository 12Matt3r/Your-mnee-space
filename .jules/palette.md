## 2024-05-22 - Standardized Loading States
**Learning:** Standardizing loading states in base components (like `Button`) drastically reduces boilerplate and inconsistency in consumer pages (like `LoginPage`).
**Action:** Always check base UI components for missing "standard" interactive states (loading, disabled, focused) before implementing custom solutions in pages.
